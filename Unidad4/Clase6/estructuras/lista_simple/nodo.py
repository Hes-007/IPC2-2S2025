#aca comenzamos a crear nuestra primera clase
class Nodo:
    def __init__(self, valor):
        self.valor = valor
        self.siguiente = None

        #ustedes cada nodo en una lista simple, el valor puede ser cuaquiera es una lista generica
        #le pueden meter numeros, objetos, ota lista, etc

        #además tenemos un self.siguiente que creamos un apuntador y siempre va a puntar a nulo

        #esto como se ve graficamente